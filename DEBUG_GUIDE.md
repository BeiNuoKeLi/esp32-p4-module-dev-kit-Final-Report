# 调试方法论：数据流分界实验

> 创建于 2026-06-15，源自 MJPEG 黑屏排查（19 vs 1 的教训）

---

## 核心原则

**在代码的边界处插日志，不在推理中兜圈。永远在看到数据之前不改逻辑。**

---

## 三步定位法

```
┌──────────┐      ┌──────────┐      ┌──────────┐
│  数据源   │ ──→  │  服务端   │ ──→  │  客户端   │
│ESP32/CAM  │      │ Python   │      │  JS/浏览器 │
└──────────┘      └──────────┘      └──────────┘
     A                 B                  C
```

### 探针 A — 数据到达没？

放在数据入库/入缓存处：

```python
print(f"[IN] frame_id={fid} size={len(data)}", flush=True)
```

- 没输出 → 问题在数据源或网络
- 有输出 → 继续 B

### 探针 B — 数据发出去没？

放在 yield / return / send 之前：

```python
print(f"[OUT] seq={seq} size={len(payload)}", flush=True)
```

- 有 A 无 B → 问题在服务端处理逻辑
- A + B 都正常 → 跳到 C

### 探针 C — 客户端收到了什么？

```javascript
console.log('[RX]', chunk.byteLength, 'bytes');
```

- 收到但显示异常 → 客户端渲染/解析 bug
- 没收到 → 网络/协议问题

---

## 实战案例：MJPEG 黑屏 (2026-06-15)

| 探针 | 位置 | 结果 | 结论 |
|---|---|---|---|
| A | ESP32→VPS | `fps=15.3` 分片正常 | ✅ 数据源正常 |
| B | `mjpeg_stream()` yield 前 | `seq 连续 new=True size=7-15KB` | ✅ 服务端正常 |
| C | 浏览器 img 显示 | 黑屏 | → **前端 bug** |

**从加探针 B 到锁定方向：< 2 分钟。**
此前在服务端挖了 19 个 commit（暗帧过滤、亮度检测、SOI/EOI 校验……）全部在错误方向上。

---

## 铁律

1. **先加探针，再改代码。** 永远不要在没看到数据的情况下改逻辑。
2. **探针放在边界线上。** 不在函数中间打洞，只在入参/返回值/Yield 点。
3. **探针正常，立刻跳到下一段。** 别在已验证正常的段落浪费 commit。
4. **`flush=True` 必须加。** 缓冲输出的日志在崩溃/卡死时看不到，误判方向。
5. **一次只加一个探针。** 加完 → 跑实验 → 确认 → 再加下一个。拒绝"洒满全文件再排查"。

---

## 快速模板

```python
# === 探针 A: 数据入口 ===
print(f"[DIAG-IN] {key}={val}", flush=True)

# === 探针 B: 数据出口 ===
print(f"[DIAG-OUT] {key}={val}", flush=True)

# 实验完成后立即删除，恢复干净代码
```

---

> 📌 提示：这不是只有这个项目才用的方法。任何遇到"不知道问题在哪一层"的场景，先插 2-3 个探针跑一遍，不要凭直觉改代码。
