# VGA (640x480) 调试记录

> 时间：2026-06-15 | 结论：VGA 分辨率下黑屏闪烁难以根除，回退到 HVGA (480x320)

---

## 实验参数

| 参数 | HVGA (稳定) | VGA 测试值 |
|------|-----------|---------|
| 分辨率 | 480×320 | 640×480 |
| JPEG Quality | 15 | 20 |
| 帧大小 | ~8KB | ~15-21KB (正常) / ~8-10KB (暗帧) |
| UDP Chunk | 1024 | 1280 |
| 分片数 | ~8 | ~13-17 |
| 目标 FPS | 15 | 15 |

---

## 调试过程

### 阶段 1：字节大小暗帧过滤 (失败)
- **发现**：ESP32 自动曝光振荡 → 周期产生 8-10KB 欠曝暗帧（正常帧 15-21KB）
- **尝试**：字节大小阈值 12KB 拦截暗帧
- **结果**：❌ 漏检严重，12-15KB 的暗帧仍通过；8.3KB 暗帧正确拦截但"保留旧帧"策略造成画面冻结

### 阶段 2：OpenCV 像素级亮度检测 (精准但无暗帧)
- **实现**：`IMREAD_REDUCED_GRAYSCALE_4` 缩略解码 + `np.mean()` 计算画面亮度均值
- **阈值**：<30/255 判定暗帧
- **诊断采样**：每 10 帧输出亮度 + EMA 均线
- **结果**：所有采样帧亮度 47-85/255，**未检测到任何真正暗帧**

### 阶段 3：JPEG 完整性校验加强
- SOI (FFD8) + EOI (FFD9) 双重检查
- **结果**：无损坏帧检测到

### 阶段 4：排除法定位
- UDP 丢帧率极低（1/2000），FPS 稳定 15
- 所有帧亮度正常（~71/255均值线）
- 但浏览器仍显示"黑屏闪"

---

## 根因推断

VGA 分辨率下黑闪可能原因（未最终确认）：

1. **浏览器客户端解码瓶颈**：VGA 帧 15-21KB → MJPEG 解码负载增加 → 浏览器掉帧/黑闪
2. **JPEG 编码器限制**：ESP32 硬件编码器在 VGA 下可能产生不符合标准的 JPEG → 浏览器容错渲染黑屏
3. **ESP32 内存压力**：VGA 帧缓冲（~300KB）接近 PSRAM 极限 → DMA 竞争导致写入残帧
4. **WiFi 分片压力**：17 个分片/帧 vs HVGA 的 8 个 → 丢包概率成倍增加
5. **自动曝光振荡**：VGA 下曝光收敛更慢，欠曝帧更多

---

## VPS 侧改进（已保留）

以下改进与分辨率无关，回退后仍有效：

| 改进项 | 说明 |
|--------|------|
| OpenCV 像素亮度检测 | `_is_dark_frame()` 替代字节大小启发式 |
| JPEG SOI+EOI 双重校验 | 拦截头尾损坏的无效 JPEG |
| 亮度采样诊断 | 每 10 帧输出亮度 + EMA 均线，快速定位问题 |
| IMREAD_REDUCED_GRAYSCALE_4 | 1/4 缩略解码，速度 ~5-10ms |

---

## 结论

**HVGA (480×320) @ quality=15 是当前跨海互联网推流的稳定分辨率上限。**

### 📌 2026-06-15 最终定位：黑屏根因不在后端

经过 19 次服务端 commit + MJPEG 流诊断日志实验，最终确认**服务端产出的每一帧都是合法的 JPEG 数据**（seq 连续、size 8-15KB、stream=true）。黑屏的真正根因是**前端 JS 解析的两个 Bug**：

1. **首帧被丢弃**：搜索串 `\r\n--frameboundary` 无法匹配流首的裸 `--frameboundary`
2. **帧尾裁切错误**：`buf.slice(hdrEnd, buf.length - 2)` 在多帧共缓冲时夹带了下一帧的 boundary + header，导致 JPEG 损坏 → 浏览器解码失败 → 黑屏

**修复**（`dashboard.html`）：改用 `--frameboundary`(15B) 搜索 + 下一个 boundary 位置定界帧尾。

> 教训：服务端一切正常时，先做数据流分界实验（print 日志），避免在后端挖洞数小时。
