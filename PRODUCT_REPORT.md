# 智慧农资仓储环境监测系统 — 产品汇报文档

> **版本**：v3.4 — 仿真注入 + 报警管理增强
> **日期**：2026-06-13
> **适用场景**：农资化肥仓库（氮肥/复合肥/钾肥）

---

## 一、产品定位

### 一句话描述
**用最少硬件**（一套环境传感器 + OLED 屏幕），实现对化肥仓库的**环境静默监测 + 分级报警 + 应急排风联动**。

### 适用仓库类型

| 仓库类型 | 典型存储物品 | 监测重点 |
|---------|-------------|---------|
| 农资化肥库 | 碳酸氢铵、尿素、硫酸铵、磷酸二铵(DAP)、氯化钾(MOP)、NPK复合肥 | NH3泄漏 → MQ-135 响应 + 温湿度防潮结块 |

### 核心设计理念

> **氨气浓度超标 → 必须排风保命；环境异常（湿度/光照）→ 提醒就行。**

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      ESP32-P4 边缘监测终端                              │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  DHT11  DS18B20  MQ-135  光敏  ──→  FreeRTOS 多任务               │  │
│  │                                               ↓                   │  │
│  │                                   ┌───────────────────────┐       │  │
│  │                                   │   OLED 本地显示        │       │  │
│  │                                   │   声光报警 (Buzzer/LED)│       │  │
│  │                                   │   继电器控制           │       │  │
│  │                                   │   仿真注入 (UDP:8081)  │       │  │
│  │                                   └───────────┬───────────┘       │  │
│  │                                               ↓                   │  │
│  │                              UDP:8080 ──→ Docker 服务              │  │
│  │               ← UDP:8081 仿真命令注入 (演示/测试用)                │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                          │ UDP:8082 (摄像头)
                          ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      Docker Web 仪表盘 (v3.0)                          │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────┐    │
│  │   FastAPI       │  │   AIOSQLite     │  │   WebSocket         │    │
│  │   REST API      │  │   数据持久化    │  │   实时推送          │    │
│  └────────┬────────┘  └────────┬────────┘  └──────────┬──────────┘    │
│           │                   │                      │                 │
│           ↓                   ↓                      ↓                 │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │               Chart.js 前端仪表盘                               │   │
│  │   传感器卡片 + 报警状态 + 历史曲线 + 摄像头流 + 仓储管理         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 Docker 服务组件

| 组件 | 说明 |
|------|------|
| **FastAPI** | REST API 后端，接收传感器数据、处理出入库操作、报警管理、仿真命令注入 |
| **AIOSQLite** | 异步 SQLite 数据库，存储传感器数据、库存、操作日志、报警事件 |
| **WebSocket** | 实时推送通道，传感器数据变更即时通知前端 |
| **Chart.js** | 前端图表库，展示温度/湿度历史趋势曲线 |
| **camera_server** | 摄像头服务：HTTP 拉取 / Push 直推 (VPS) / UDP 中继 (备选) → MJPEG 流转换 + Canvas 快照轮询 + pyzbar 二维码扫码，支持 stream 开关暂停/恢复，关闭时前端自动切黑屏 |

### 2.3 后端 API 路由总览

| 方法 | 路由 | 功能 |
|------|------|------|
| `POST` | `/api/sensors` | 接收 ESP32-P4 传感器数据（HTTP 备选通道） |
| `GET` | `/api/status` | 获取当前最新传感器状态 |
| `GET` | `/api/sensors/history` | 传感器历史数据查询 |
| `POST` | `/api/warehouse/checkin` | 入库操作 |
| `POST` | `/api/warehouse/checkout` | 出库操作 |
| `GET` | `/api/warehouse/log` | 出入库流水查询 |
| `DELETE` | `/api/warehouse/log` | 清空全部出入库流水 |
| `GET` | `/api/inventory` | 查询库存列表 |
| `DELETE` | `/api/inventory/{item_id}` | 删除指定库存物料 |
| `POST` | `/api/inventory/add` | 手动新增库存物料 |
| `GET` | `/api/env-snapshot` | 当前环境快照（扫码时自动填充） |
| `GET` | `/api/camera/mjpeg` | 摄像头 MJPEG 实时流 |
| `GET` | `/api/camera/snapshot` | 摄像头最新帧 JPEG |
| `GET` | `/api/camera/status` | 摄像头运行状态 |
| `GET` | `/api/camera/stream` | 查询视频流开关状态 |
| `POST` | `/api/camera/stream` | 切换视频流开关 |
| `POST` | `/api/camera/scan` | 摄像头帧二维码扫码 |
| `GET` | `/api/alarms` | 报警历史分页列表（支持 level 筛选） |
| `GET` | `/api/alarms/summary` | 报警概要统计 |
| `GET` | `/api/alarms/{id}` | 报警详情（含现场快照） |
| `POST` | `/api/alarms/{id}/ack` | 确认单条报警 |
| `DELETE` | `/api/alarms` | 清空全部报警记录 |
| `GET` | `/api/alarm/config` | 获取当前报警配置（AO/DO 模式、阈值） |
| `POST` | `/api/alarm/config` | 更新报警配置 → 写 SQLite + UDP 同步到 MCU |
| `GET` | `/api/sim/status` | 查询仿真注入状态 |
| `POST` | `/api/sim/inject` | 注入仿真传感器数据到 ESP32-P4 |
| `POST` | `/api/camera/push` | ESP32-CAM 直推 JPEG 帧 (VPS 部署) |

### 2.4 摄像头数据流

#### Push 模式：ESP32-CAM 直推 VPS (服务器部署推荐)

```
ESP32-CAM ──HTTP POST──► VPS :8001 /api/camera/push  (CAMERA_MODE=push)
  │   每 200ms 推一帧 (~5fps)      ↓
  │   JPEG raw body           Docker camera_server
  │   CameraWebServer.ino          ↓
  └──────────────────────  Web 仪表盘实时显示
```

#### 主模式：Docker 直连拉取 (局域网推荐，TCP 零丢包)

```
ESP32-CAM ──HTTP/TCP──► Docker camera_server  (CAMERA_MODE=http)
  │   /capture + /stream           ↓
  │   HVGA 480×320, q=12      MJPEG 流 + Canvas 快照轮询 + 二维码解码
  │                                 ↓
  └──────────────────────  Web 仪表盘实时显示
```

#### 备选模式：ESP32-P4 UDP 中继 (已弃用，默认关闭)

```
ESP32-CAM ─HTTP:/capture─→ camera_http_fetch.c ─UDP:8003─→ Docker camera_server
  (备用，端口 8003→8082/udp)                                    ↓
                                                    MJPEG 流 + 二维码解码
```

---

## 三、报警策略：分级智能决策

### 3.1 报警源两分类

| 类别 | 传感器 | 触发条件 | 含义 | 控制动作 |
|------|--------|---------|------|---------|
| **A 类 — 风机关联** | MQ-135 AO/DO | DO=0 或 AO 超阈值（氨气超标） | 化肥分解 → 氨气扩散 | 蜂鸣 + 红灯 + **风扇 ON** |
| **A 类 — 风机关联** | DHT11 温度 | ≥ 阈值（默认 35°C） | 高温加速氨挥发/自燃风险 | 蜂鸣 + 红灯 + **风扇 ON** |
| **A 类 — 风机关联** | DS18B20 温度 | ≥ 阈值（默认 35°C） | 高精度高温检测 | 蜂鸣 + 红灯 + **风扇 ON** |
| **B 类 — 仅提醒** | 光敏 AO/DO | DO=0 或 AO 超阈值（光照异常） | 光照可能加速化肥分解 | 蜂鸣 + 红灯，**风扇不动** |
| **B 类 — 仅提醒** | DHT11 湿度 | ≥ 85%RH | 化肥受潮结块/养分流失 | 蜂鸣 + 红灯，**风扇不动** |

### 3.2 四级响应体系

| 级别 | 触发条件 | 蜂鸣器 | 红灯 | 排风扇 | 业务含义 |
|------|---------|--------|------|--------|---------|
| **L0 · 正常** | 无传感器触发 | 静音 | 灭 | OFF | 仓库环境合规，无需干预 |
| **L1 · 预警** | 仅 B 类源触发 | 200ms 鸣 / 800ms 停 | 慢闪 (500ms) | **OFF** | 光照/湿度异常但非毒气，通知管理员检查 |
| **L2 · 严重** | 任意单个 A 类源触发 | 300ms 鸣 / 300ms 停 | 快闪 (200ms) | **ON** | 毒气泄漏或高温出现，排风降浓度 |
| **L3 · 紧急** | 多个 A 类源同时触发 或 报警持续 >30s | 长鸣不休 | 常亮 | **ON** | 多重危险交叠或险情持续，紧急排风 |

### 3.3 为什么这样设计？

| 决策 | 理由 |
|------|------|
| **为什么要分级？** | 二元报警不够精细：管理员需要区分"去看看就行"和"立刻疏散"。 |
| **为什么光敏/湿度不开风扇？** | 光照、湿度异常不是气体扩散问题，开排风扇无意义，甚至可能加速化学品挥发。 |
| **为什么氨气/高温开风扇？** | 碳酸氢铵等易分解化肥泄漏后，关闭风扇会让氨气在密闭仓库内积累至危险浓度。排风是唯一正确的应急手段。 |
| **为什么 L3 要 30 秒升级？** | 防止短暂误报触发紧急模式；30 秒持续报警说明真实泄漏未解除，需升级响应。 |
| **正常状态为什么关风扇？** | 无人值守时关风扇节能；只有在检测到风险时才启动排风。 |
| **为什么 30 秒去重写库？** | 同一级别持续报警若每秒写入会产生大量冗余记录；30 秒窗口内同级别仅记一条，级别变化则立即写入。 |

### 3.4 报警历史管理

| 功能 | 说明 |
|------|------|
| **分页查询** | 支持按级别（L1/L2/L3）筛选，每页 20 条 |
| **统计概要** | 实时显示总报警数 + 未确认数 |
| **详情弹窗** | 点击单条记录弹窗展示完整传感器快照 + 现场 JPEG 图片 |
| **确认机制** | 管理员可确认单条报警（acknowledged=1），未确认行红色高亮 |
| **一键清空** | `DELETE /api/alarms` 清空全部报警记录（需二次确认） |
| **去重策略** | 同一级别 30 秒内仅记录一条；级别变化立即记录 |
| **现场快照** | 每次报警事件自动抓取摄像头当前帧（BLOB 存储，详情页 base64 渲染） |

### 3.5 运行时报警配置（v3.5）

> 无需重新编译或重启 ESP32，所有报警参数均可在 Web 仪表盘上动态调整。

| 特性 | 说明 |
|------|------|
| **AO/DO 双模式** | MQ-135 和光敏传感器各自独立选择 DO（硬件比较器）或 AO（软件阈值判定）模式 |
| **AO 阈值可调** | MQ-135 电压阈值（0~3.3V）、光敏 ADC 阈值（0~4095）通过滑块实时调整 |
| **触发方向** | 支持"高于阈值"（MQ-135 毒气检测）或"低于阈值"（光敏遮挡检测）两种方向 |
| **温湿度独立开关** | 温湿度报警可整体关闭，仅保留气体+光敏报警 |
| **端到端同步** | Web → Docker SQLite → UDP 8081 → ESP32 NVS 持久化，断电不丢失 |
| **范围保护** | 启动时 NVS 加载带范围校验，垃圾值自动回退默认值 |

---

## 四、仿真注入系统（v3.4 新增）

### 4.1 设计目的

> 在汇报演示或系统调试时，无需实际改变仓库环境即可复现各级报警场景。

### 4.2 数据流

```
Web 仪表盘                               ESP32-P4 (MCU)
  │                                          │
  │  POST /api/sim/inject                    │
  │  {"photo_raw":2000, "mq135_v":2.8, ...}  │
  ├────────────────── UDP:8081 ──────────────►│
  │                                          ├─ udp_sim_command_task() 解析
  │                                          ├─ 写入 g_sensor_data.sim_xxx
  │                                          ├─ sim_active = 1 (切换仿真模式)
  │                                          │
  │              ← UDP:8080 ──────────────── │ (仿真数据作为真实传感器值上报)
  │  {"level":2, "mq135_do":0, ...}         │  传感器任务读取 sim_xxx → 报警逻辑正常运作
  │                                          │
  ▼                                          │
  前端自动更新报警状态 + 环境数据
  (与真实传感器行为完全一致)
```

### 4.3 关键特性

| 特性 | 说明 |
|------|------|
| **模式切换** | 发送 `{"reset":""}` 命令退出仿真，恢复真实传感器 |
| **字段可选** | 注入命令中任意字段可省略，省略字段使用当前真实值 |
| **无缝衔接** | 仿真数据走完整传感器→报警逻辑链路，模拟真实触发效果 |
| **快速预设** | 前端内置 L1/L2/L3 三个预设按钮，一键注入典型报警场景 |
| **状态可见** | 前端标签显示"仿真注入 (MCU处理中…)"，清楚区分真/仿模式 |

### 4.4 前端预设

| 预设 | dht11_t | dht11_h | ds18b20_t | mq135_v | mq135_do | photo_do | 预期级别 |
|------|---------|---------|-----------|---------|----------|----------|----------|
| L1 预警 | 25 | 86 | 25.0 | 1.2 | 1 | 0 | L1（光敏+湿度） |
| L2 严重 | 39 | 60 | 25.0 | 2.8 | 0 | 1 | L2（MQ135+高温） |
| L3 紧急 | 39 | 60 | 38.5 | 2.8 | 0 | 1 | L3（多A类源） |

### 4.5 API 端点

| 方法 | 路由 | 功能 |
|------|------|------|
| `GET` | `/api/sim/status` | 查询 ESP32-P4 是否处于仿真模式 |
| `POST` | `/api/sim/inject` | 注入仿真传感器值到 ESP32-P4 |
| `POST` | `/api/sim/reset` | 退出仿真，恢复真实传感器 |

---

## 五、通信协议

### 5.1 JSON 报文格式（v2.0）

```json
{
  "type": "data",
  "level": 2,
  "ts": 120000,
  "dht11_t": 39.0,
  "dht11_h": 62.0,
  "ds18b20_t": 28.3125,
  "mq135_v": 1.45,
  "light_raw": 1500,
  "alert": 1,
  "err": 0,
  "reason": "mq135, dht11_temp"
}
```

| 字段 | 类型 | v2.0 说明 |
|------|------|-----------|
| `type` | string | **新增**。当前固定 `"data"`，预留 `"checkin"`、`"alert_image"` |
| `level` | int | **新增**。0=正常, 1=预警, 2=严重, 3=紧急 |
| `ts` | int | FreeRTOS 启动后毫秒时间戳 |
| `alert` | int | 兼容旧字段，1 表示任一报警源触发 |
| `reason` | string | 具体触发原因枚举 |
| 其他 | — | 传感器原始值，字段不变 |

### 5.2 PC 上位机显示效果

```
L0 (正常):    绿色文字，无警告标记
L1 (预警):    黄色高亮  [⚠ 预警] 光敏异常
L2 (严重):    橙色高亮  [⚠ 严重] MQ-135 毒气超标
L3 (紧急):    红色闪烁  [🚨 紧急] 多重危险 - 立即排风
```

---

## 六、硬件清单

| 设备 | 型号/规格 | 数量 | 用途 |
|------|----------|------|------|
| MCU | ESP32-P4 RISC-V 双核 | 1 | 边缘计算 + WiFi 通信 |
| 温湿度 | DHT11（数字） | 1 | 仓库环境温湿度 |
| 高精度温度 | DS18B20（1-Wire） | 1 | ±0.5°C 高精度温度备份 |
| 有害气体 | MQ-135（AO + DO） | 1 | 氨气/硫化物检测 |
| 光照 | 光敏电阻（AO + DO） | 1 | 光照异常检测 |
| 显示 | OLED SSD1306 128x64 | 1 | 本地数据可视化 |
| 蜂鸣器 | 有源蜂鸣器 + S8050 | 1 | 声报警 |
| LED | 共阴极红绿双色 | 1 | 光报警（红=报警，绿=正常） |
| 继电器 | 5V 单路模块 | 1 | 控制排风扇 |
| WiFi | ESP32-C6（SDIO 协处理器） | 1 | 局域网无线通信 |

---

## 七、业务价值总结

| 维度 | 价值 |
|------|------|
| **安全** | 氨气超标/高温自动排风，分级报警避免"狼来了"效应 |
| **溯源** | 报警事件持久化到 SQLite，附带现场摄像头快照，事后可回溯事故时间线 |
| **节能** | 正常时风扇关闭，仅在检测到风险时启动排风 |
| **可演示** | 仿真注入系统支持一键复现 L1/L2/L3 各级报警，汇报时无需改变真实环境 |
| **可扩展** | JSON `type` 字段预留入库扫码和抓拍图片类型，后续无缝升级 |
| **低成本** | 全部使用通用传感器模块，物料成本低，易于批量部署 |

---

## 八、仓储管理功能（v2.2 新增）

### 8.1 二维码标签设计

每袋化肥包装上贴有二维码标签，内含 JSON 格式的物料身份信息：

```json
{"id":"FERT-20260611-001","name":"碳酸氢铵","category":"化肥","batch":"B2026-0611","spec":"50kg/袋","mfg_date":"2026-06-01","exp_date":"2027-06-01","sensor_note":"MQ-135(NH3直接检测) + DHT11(高温高湿加速分解)"}
```

### 8.2 入库/出库流程

| 步骤 | 操作 | 系统行为 |
|------|------|---------|
| 1 | 切换到「仓储管理」Tab | 自动启动摄像头 UDP 8082 拉流 |
| 2 | 点击「入库模式」或「出库模式」 | 界面高亮当前模式，等待扫码 |
| 3 | 将二维码对准 ESP32-CAM | pyzbar 实时解码，2 秒冷却防重复 |
| 4 | 扫码成功 | 物料信息 + 当前环境快照（温度/湿度/级别）写入 SQLite |
| 5 | 库存表格自动刷新 | TreeView 绿色=在库、灰色=已出库 |

> **摄像头预览**：点击顶部「打开摄像头预览」按钮，弹出独立 Toplevel 窗口显示 640×480+ 实时画面，独立于主界面运行。

### 8.3 传感器—化肥匹配矩阵

| 物料 | MQ-135 | DHT11(温) | DHT11(湿) | 光敏 |
|------|:------:|:---------:|:---------:|:----:|
| 碳酸氢铵 | ✅ NH3直接检 | ⚠️ 高温加速分解 | ⚠️ 高湿加速分解 | — |
| 尿素 | ✅ 氨挥发 | — | ⚠️ 吸潮结块 | — |
| 硫酸铵 | ✅ NH3检测 | — | ⚠️ 酸性肥料,湿度敏感 | — |
| 磷酸二铵(DAP) | ✅ NH3检测 | — | ⚠️ 湿度→结块,养分流失 | — |
| 氯化钾(MOP) | — | — | ⚠️ 纯湿度敏感,易潮解 | — |
| NPK 复合肥(15-15-15) | ✅ NH3检测 | ⚠️ 温度敏感 | ⚠️ 湿度敏感 | — |

> 每个物料至少有一个传感器能在异常时捕获信号，实现 **"传感器—化肥"闭环**。

---

## 九、下一步规划

| 阶段 | 内容 | 状态 | 完成日期 |
|------|------|------|----------|
| 阶段三 | 接入摄像头 → ESP32-CAM HTTP 拉图 + UDP 中继转发 | ✅ | 2026-06-07 |
| 阶段四 | 二维码仓储管理 → SQLite 入库/出库 + pyzbar 扫码 | ✅ | 2026-06-07 |
| 阶段五 | DHT11 时序修复 → 脉冲宽度测量替代固定延时 | ✅ | 2026-06-07 |
| 阶段六 | Web 可视化仪表盘（FastAPI + Chart.js + Docker） | ✅ | 2026-06-09 |
| 阶段七 | 多仓库节点集中管理平台 | ⏳ | 规划中 |
| 阶段八 | 摄像头帧率优化 → HVGA 480×320 + Docker 直连 TCP 架构 | ✅ | 2026-06-11 |
| 阶段九 | 仿真注入 + 报警管理增强 + 前端交互优化 | ✅ | 2026-06-13 |

### 9.1 已完成功能清单（v3.4）

**Web 仪表盘功能**：
- ✅ FastAPI REST API（传感器数据、库存管理、摄像头控制、报警管理、仿真注入）
- ✅ WebSocket 实时推送（传感器数据即时更新）
- ✅ Chart.js 历史趋势曲线（温度/湿度，Y 轴固定物理量程）
- ✅ 分级报警状态展示（L0~L3，L3 紧急时页面红色闪烁 + body 红色脉冲背景）
- ✅ **报警历史系统**（分页列表 + 级别筛选 + 详情弹窗 + 现场快照 + 确认 + 一键清空）
- ✅ 报警去重策略（同级别 30s 内仅记录一条，级别变化立即写入）
- ✅ MQ-135/光敏 DO 数字输出状态实时显示（正常=绿色 / 报警=红色闪烁）
- ✅ 摄像头 MJPEG 实时流预览 + **视频流开关**（关闭时自动切黑屏，不消耗带宽）
- ✅ 二维码扫码入库/出库（化肥标签 FERT-20260611-001~006）+ CLAHE 增强
- ✅ **仿真注入系统**（Web → ESP32-P4 UDP 命令，一键复现 L1/L2/L3 报警场景）
- ✅ 待机模式 / 出入库模式（UI 状态切换，扫码按钮联动禁用）
- ✅ Toast 通知系统（操作反馈即时可见）
- ✅ Docker 容器化部署

**摄像头增强**：
- ✅ ESP32-CAM HTTP 拉图 + Docker 直连 (主模式, TCP 零丢包)
- ✅ **HVGA 480×320** 分辨率，JPEG quality=12，识别率提升
- ✅ ESP32-P4 UDP 中继 (备选, 默认关闭)
- ✅ **CameraWebServer/** ESP32-CAM Arduino 工程源码 (OV2640)
- ✅ **Canvas 快照轮询** 替代 MJPEG `<img>` (消除 Chrome 缓冲延迟)
- ✅ 工业摄像头 KYT-U400 支持
- ✅ 独立预览窗口 (Toplevel)
- ✅ 视频流关闭后 Canvas 立即黑屏（节省带宽 + 隐私保护）

**稳定性优化**：
- ✅ DHT11 时序修复（脉冲宽度测量，解决 ~95% 失败率）
- ✅ UDP ENOMEM 退避重试机制
- ✅ Wi-Fi 连接轮询等待
- ✅ **HTTP 超时 10s / Content-Length ≤0 拦截 / EOF 边界修复**（camera_http_fetch.c）
- ✅ JS try-catch 语法修复（消除 `Missing catch or finally after try` 运行时错误）
- ✅ Canvas Blob URL 内存泄漏修复（onload/onerror 均释放 URL.revokeObjectURL）

---

> **最后更新**：2026-06-13
> **作者**：SmartMonitor Team
